
// routes
import { PATH_DASHBOARD } from './routes/paths';


const Config   = {
    APP_NAME: "ADCL",
    API_URL: "http://localhost:8000",
    PATH_AFTER_LOGIN: PATH_DASHBOARD.affaire.root,
};

export default Config;